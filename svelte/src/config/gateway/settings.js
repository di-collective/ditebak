const config = {
    login: process.env.GW_LOGIN,
    logout: process.env.GW_LOGOUT,
    topics: process.env.GW_TOPICS,
    bets: process.env.GW_BETS,
    profile: process.env.GW_PROFILE
}

export { config }